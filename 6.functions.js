
//Anatomy
/*
    - Add = function name
    - (a, b) = parameters
    - {return a + b} = Function Body
    - add(3,4,5) = Invocation/Call-Time
    - (3,4,5) = Arguments
*/

let add = function(a, b){
    return a + b;
}

add(3,4,5);

/*
    - Function Definition = function(...){...}
    - Function Name = let nameFunction = ...
    - Invocation = nameFunction() - example: log(), hide(), forEach(), $()...
    - Function Body = Inside de {...} and they dont get invoked until the function is called.
    - Invocations = ()
    - Arguments = arguments contain the value of the variables (parameters)
    - Side Effects = Anything that reaches beyond the curly brackets of a function
*/

// Arrow Functions
